server:
  port: 8081

spring:
  application:
    name: product-service
  data:
    mongodb:
      # Nếu dùng Docker Desktop, hãy đảm bảo MongoDB đang chạy ở port 27017
      uri: mongodb://localhost:27017/product-db 

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/